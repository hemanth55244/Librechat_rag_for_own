services:
  fastapi:
    build: .
    environment:
      - VECTOR_DB_TYPE=atlas-mongo
      - EMBEDDING_BATCH_SIZE=${EMBEDDING_BATCH_SIZE:-500}
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
    env_file:
      - .env
